void BFS(S,G) { 
  queue<int> Q;
  S[visited] = true;
  Q.push(S);
  while(!Q.empty()) {
    n = Q.pop();
    for ever neigh of n:
      if(neigh.visited) {
        Q.push(neigh);
        neigh.visited =true;
      }
  }
}
